# Legacy Documentation

These reports describe the pre-Supabase Firebase/Firestore architecture and readiness state. They are retained for historical reference only and should not be used as the current setup or deployment source of truth.

Current setup and database bootstrap guidance lives in the repository root README and the Supabase migration history under `supabase/migrations/`.
